import { Tag } from "antd";
import styled from "styled-components";

export const RoomCodeTag = styled(Tag).attrs({ color: "gold" })`
  font-size: 22px;
  line-height: normal;
  font-weight: bold;
`;
