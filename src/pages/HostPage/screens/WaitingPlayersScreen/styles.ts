import { Col } from "antd";
import styled from "styled-components";

export const WrapperInviteBlock = styled(Col)`
  min-width: 40%;
  text-align: center;
`;

export const WrapperPlayersBlock = styled(Col)`
  width: 100%;
  max-width: 60vh;
  margin: 0 auto;
`;
